# 遊び代管理アプリ 公開・インストール手順

## 必要なもの
- Windows PC（すでに持っている）
- iPhone（インストールして使う端末）
- GitHubアカウント（無料）

---

## STEP 1：GitHubにアカウントを作る（すでにある人はスキップ）

1. https://github.com を開く
2. 「Sign up」からメールアドレスで無料登録

---

## STEP 2：GitHubにファイルをアップロードする

1. GitHubにログインして、右上の「＋」→「New repository」をクリック
2. Repository name に「playbudget」と入力
3. **「Public」を選択**（重要！Privateだと公開されません）
4. 「Create repository」をクリック

5. 「uploading an existing file」のリンクをクリック
6. 以下の5ファイルをドラッグ＆ドロップ：
   - `index.html`
   - `manifest.json`
   - `sw.js`
   - `icon-192.png`
   - `icon-512.png`（アプリのアイコン画像）
7. 「Commit changes」をクリック

> **アプリを更新したとき**：同じ手順で変更したファイル（通常は `index.html` と `sw.js`、アイコンを変えたときは `icon-192.png` と `icon-512.png`）を再アップロード（上書き）してください。
> `sw.js` のキャッシュ名（`playbudget-v3` など）が変わっていれば、インストール済みのiPhoneも次回起動時に自動で新しい版に切り替わります。

---

## STEP 3：GitHub Pagesで公開する

1. リポジトリの「Settings」タブをクリック
2. 左メニューの「Pages」をクリック
3. 「Branch」を「main」に変更して「Save」

数分後、以下のURLでアクセスできるようになります：
```
https://あなたのユーザー名.github.io/playbudget/
```

---

## STEP 4：iPhoneにインストールする

1. iPhoneのSafariで上のURLを開く
2. 下のメニューバーの「共有」ボタン（□↑）をタップ
3. 「ホーム画面に追加」をタップ
4. 「追加」をタップ

→ ホーム画面にアプリのアイコンが追加されます！
→ 以後はアイコンをタップするだけで起動できます 🎉

---

## アプリの使い方

### ホーム画面
- 今月の残り予算が大きく表示されます
- カラーが 緑→オレンジ→赤 と変わって残り具合がわかります
- 支出項目の「✕」で削除できます

### 追加タブ
1. 金額を入力
2. カテゴリをタップして選ぶ
3. メモ（任意）を書く
4. 「支出を追加する」ボタンを押す

### 取り込みタブ（レシート / カード利用履歴）

**レシートを撮影して取り込む**
1. 「レシートを撮影する」ボタンを押す
2. カメラが起動するのでレシートを撮影
3. AIが金額を自動検出（初回は少し時間がかかります）
4. 合計金額をタップして選ぶ
5. カテゴリを選んで「保存する」

**カード利用履歴のスクショから取り込む**
1. カードアプリの利用履歴画面をスクショしておく
2. 「利用履歴のスクショを選ぶ」ボタンを押して、写真からスクショを選ぶ
3. 「全ての明細を取り込む」か「赤丸で囲んだ明細だけ取り込む」を選ぶ
   - 「赤丸で囲んだ明細だけ」を選ぶと、スクショが表示されるので **取り込みたい行を指で赤く囲む**（いくつでもOK）
   - 「✏️ 描く」と「✋ 移動」を切り替えてスクロール、「＋／－」で拡大縮小、「↩ 戻す」「🗑 全消し」でやり直しができる
   - 囲めたら「この赤丸で取り込む」を押す（写真アプリなどで先に赤丸を描いたスクショなら、そのまま押してOK）
4. 読み取られた明細の一覧が出るので、内容を確認する
   - 取り込まない行はチェックを外す
   - 日付・金額・店名（メモ）・カテゴリはその場で修正できる
   - 「カテゴリを一括設定」で全部まとめて変更できる
   - すでに同じ日・同額の支出がある行は「登録済みかも」と警告が出て、最初からチェックが外れています
5. 「選択した N 件を保存する」を押す

※ 取り込んだ支出には一覧で 📷（レシート）／ 💳（カード履歴）のマークが付きます。

### 設定タブ
- 月の遊び代の予算を変更できます
- カテゴリを追加・削除できます

---

## よくある質問

**Q: データはどこに保存されますか？**
A: iPhoneのSafari内にのみ保存されます。外部サーバーには送られません。

**Q: レシートの金額が正しく読み取れない場合は？**
A: 明るい場所でレシートを平らにして撮影すると改善します。読み取れない場合は「追加」タブから手動入力してください。

**Q: カード利用履歴の赤丸が検出されません**
A: 行の少し外側を、線が途切れないように囲むと検出しやすくなります。線が文字に重なると、その文字が読めなくなることがあります。写真アプリで描いた丸を使う場合は、はっきりした赤色にしてください（ピンクやオレンジは赤と判定されません）。検出できないときは「全ての明細を取り込む」を選び、一覧で不要な行のチェックを外してください。

**Q: カード利用履歴で金額が読み取れない明細があります**
A: 金額は「¥」で始まる数字だけを拾います（日付やカード番号などの数字を金額と間違えないため）。OCRが「¥」を読み落とした行は一覧に出ないので、「追加」タブから手動で入力してください。「1,200円」のように「円」で表示するアプリのスクショは、画面内に「¥」表記が一つも無い場合だけ「円」付きの数字を金額として扱います。

**Q: 取り込んだ明細の日付や店名が違います**
A: 読み取り結果の一覧でそのまま修正できます。日付が読めなかった行には警告が表示され、隣の明細の日付を仮で入れています。

**Q: 月が変わると自動でリセットされますか？**
A: ホーム画面は今月分だけ表示します。翌月になると自動で0円からスタートします（データ自体は消えません）。

**Q: iPhoneを機種変更したらデータはどうなりますか？**
A: Safari内に保存されているため、機種変更するとデータはリセットされます。

**Q: Chromeでは使えますか？**
A: PCのChromeでも動きますが、ホーム画面への追加はiPhoneのSafariでのみ可能です。
